export const schema = `
Table: users
Columns:
- id (integer)
- name (text)
- email (text)
- city (text)
- created_at (timestamp)
`;

//This code defines a string constant named "schema" that describes the structure of a database table called "users". The table has five columns: id, name, email, city, and created_at, along with their respective data types.
//This schema can be used as a reference for generating SQL queries or for documentation purposes.